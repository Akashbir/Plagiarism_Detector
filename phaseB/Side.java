/**
 * An enum to represent a Side. The plagiarism detection is only supported for two projects at a
 * time, so side will help differentiate the two projects. The term side was chosen since it
 * reflects where the project would visually appear in the application.
 */
public enum Side { LEFT, RIGHT; }
