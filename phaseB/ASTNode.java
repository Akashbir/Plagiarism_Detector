/**
 * An interface to represent an ASTNode.
 */
public interface ASTNode {

    /**
     * Method to get a string representation of the data stored in this node.
     *
     * @return String, representing the data at this node
     */
    String getText();
}
